#include <iostream>
#include <vector>
using namespace std;


// 문자열을 주면 괄호만 남긴다
string remainBracket(string str) {
	
	return "";
}

string isPairBracket(string str) {
	
	return "";
}

int main() {
	int N;
	cin >> N;
	
	vector<string> strArr;
	
	// 문자열들 입력받기
	for (int ni = 0; ni < N; ni++) {
		string str;
		cin >> str;
		strArr.push_back(str);
	}
	
	for (int ni = 0; ni < N; ni++) {
		string str = strArr[ni];
		
		string onlyBracket = remainBracket(str);
		
		
	}
	
}


/*
 
 */
